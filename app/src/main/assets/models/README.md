Place optimized GLB files here for offline 3D visualization.

Expected names:

- `bottle.glb`
- `book.glb`
- `cup.glb`

Keep mobile AR assets small:

- Under 50k triangles where possible.
- Texture sizes at 1024px or lower for common devices.
- Prefer a single self-contained `.glb` file.
