# Integration Tests

Use `app/src/androidTest/java/com/rola/app/integration` for Room migration tests, Firestore sync tests, AR-to-ML pipeline tests, and voice lifecycle tests on Android devices or emulators.
